{
  "mcpServers": {
    "persona": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PROJECT_DIR:-.}",
        "python",
        "${CLAUDE_PROJECT_DIR:-.}/persona_runtime.py",
        "mcp"
      ]
    }
  }
}
