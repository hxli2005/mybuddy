# mini 分支 —— Agent 工作守则

> 现役产品:开源 MCP 人格运行时——为 Claude Code 等宿主提供有出处的长期人格;
> 单文件、三个 JSON、无数据库。旧桌面链路(mybuddy/、buddyshell/)冻结保留,
> 是未来的第二宿主(M5),不是施工对象。CLAUDE.md 与 AGENTS.md 逐字节一致。

## 教训(为什么这样设计)

v3 用 5 天长成 20 万行"测试全绿"的哑巴;mini 又为对抗测试长出上千行语言审查,
2026-07-24 所有者裁决退役。两次教训同一句话:**智能住在模型里,引擎只管世界。
文档不接受评审——只有跑着的代码和真实轨迹接受评审。**测试全绿 ≠ 完成;
计划完美 ≠ 开工。

## 哲学(乐观约束,所有者裁决)

- **管世界,不管嘴**:引擎保证记忆只由真实发生的事生成;说什么话是宿主模型的
  事,prompt 承载人格,代码永不审查措辞。
- **错误即反馈**:校验失败逐条丢弃,拒因作为返回值交宿主自纠;不整包拒绝、
  无静态接住、不吞异常。
- **划界**:引擎不做项目知识库;工程事实归宿主自己的记忆工具,这里只记关于人
  的事实、共同经历、persona 自身经历与模式。

## 红线(两条,实现为一处世界校验,管全部写入)

**世界不说谎**(不编造+不撤回+无总分):每条记忆必须有真实事件出处;已发生的
只能公开纠正,不能抹掉;永不存在关系分。**沉默零债务**:未回应与离开不写入
任何状态、记忆或惩罚。

## 品味(约束 persona,不约束引擎)

小布是桌面纯陪伴 persona,休眠中:不做任务、她自己的生活是核心欲望、亲密浓度
不固化、记忆不从 v3 迁移。引擎与 persona 无关;公开发布的 persona 名不用
"小布"(S21 未解)。

## 尺寸与风格

- 新运行时单文件目标 ≤400 行、守卫 500(数字仅所有者可改);人格宪法/记忆/
  文档是数据,不计行数。行数由删除产生,禁止行数高尔夫,可读性优先。
- JSON 文件态、单进程、单写者、临时文件替换写入;失败与拒因保存原文。不建:
  数据库、向量库、队列、worker、事件溯源、依赖注入、多实例框架。
- 代码、标识符、错误信息全英文,逻辑语言中立;宪法与记忆内容任何语言皆可,
  引擎不感知。
- 教科书标准,对标 nanoGPT/tinygrad/pi:单文件从上到下讲一个故事(数据结构→
  世界校验→工具→持久化→入口);需要注释才看懂的代码应重写而非注释。
- 反防御、反抽象:坏输入明确失败,防护只存在于世界校验一处;没有第二个调用者
  不抽象;两个方案选小的,能删不加。
- 测试是规格不是攻击面:只钉世界不变量;禁止对抗语料堆积与特判补丁;同一问题
  第二次打补丁 = 停下重新设计。

## 完成的定义

改动 → 跑起来 → 宿主真实说出的话贴进交接 → 一句话回答"哪里更活了/更可信了"。

## 多 session 协作

- 开工顺序固定:读本文件 → `DESIGN.md` → `WORK.md` → `git status`和最近提交。
- `DESIGN.md`管产品与架构,`WORK.md`只管当前任务和最近交接,Git保存历史;不得
  另建计划、状态、重构或评审文档。
- 同一工作区同一时刻只有一个编码 session 写文件;其他 session 只能只读研究或
  评审。并行编码必须使用独立 worktree、互不重叠的文件范围和独立提交。
- 编码 session 只领取 `WORK.md`中的一个 READY 任务;遇到不属于本刀的问题只记
  一行阻塞,不顺手铺接口。完成后先运行、提交代码,再用真实证据替换交接区。
- 旧桌面链路(`mybuddy/`、`buddyshell/`及其测试与回归脚本)冻结:不删除、
  不修改,不属于任何刀的文件范围;收拢或分家由 M5 之后裁决。
- 接手时若工作区有不属于本任务的未提交改动,不覆盖、不 stash、不代为整理,
  停止写入并报告;不得在脏工作区猜测上一 session 做到了哪里。
- `WORK.md`不追加流水账,只保留当前状态和最近一次交接;旧交接由 Git查看。

## 设计文档

`DESIGN.md` 是唯一设计文档,当前描述的是已冻结的桌面闭环;**永不改版评审**,
要改就直接改代码,M 刀完成后事后如实记录新运行时。主仓 docs/(宪法、
ENGINE_REBUILD_PLAN v2.3、BODY_CONTRACT)只作只读素材。
